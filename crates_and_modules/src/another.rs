//! An impressive module
//! whose function is unknown

pub mod inner;

/// Don't ask what this struct is but `13 * 12 / a`
/// ```
/// if samples::everything().works() {
///     println!("ok");
/// }
/// ```
pub struct X { // if not pub, it would be an error for the foo() function signature

}

/// Very complex function
/// Create a [`X`] which is a [`crate::plant_structures::leaves::Leaf`]
/// but you can simplify into [`Leaf`][l]
/// or if you like into [`Leaf`][crate::plant_structures::leaves::Leaf]
///
/// [l]: crate::plant_structures::leaves::Leaf
pub fn foo() -> X {
    X{}
}